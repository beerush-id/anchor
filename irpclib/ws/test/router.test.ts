// Note: The WebSocketRouter tests have been modularized into multiple files under ./router/*.test.ts
// to prevent token waste and keep test files focused.
import { describe, expect, it } from 'vitest';

describe('WebSocketRouter Test Suite Modularization Notice', () => {
  it('has been split into test/router/*.test.ts', () => {
    expect(true).toBe(true);
  });
});
