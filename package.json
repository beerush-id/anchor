{
  "name": "anchor",
  "type": "module",
  "version": "0.0.1",
  "description": "Reactive State Management for Javascript",
  "scripts": {
    "dev": "bpkg run dev",
    "dev:core": "bpkg run dev -f packages/core",
    "dev:docs": "bpkg run dev -f apps/docs",
    "dev:react": "bpkg run dev -f apps/react",
    "build": "bpkg run build",
    "format": "bpkg run format",
    "lint": "bpkg run lint",
    "test": "rimraf tests && vitest --run",
    "test:preview": "rimraf tests && vitest --run && vite preview --outDir tests",
    "prepare": "husky"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "monopkg": "^0.3.10",
    "prettier": "^3.2.5",
    "vitest": "^3.2.4"
  },
  "engines": {
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "author": "Nanang Mahdaen El Agung <mahdaen@gmail.com>",
  "homepage": "https://github.com/beerush-id/anchor",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beerush-id/anchor.git"
  },
  "bugs": {
    "url": "https://github.com/beerush-id/anchor/issues"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,scss,md}": [
      "prettier --write"
    ]
  }
}
