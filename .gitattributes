# Set default behavior to automatically normalize line endings
* text=auto

# Force specific files to have Unix line endings
*.{js,jsx,ts,tsx,json,css,scss,md,svelte} text eol=lf

# Ensure bash scripts have Unix line endings
*.sh text eol=lf

# Windows-specific files
*.bat text eol=crlf
