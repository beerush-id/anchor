import parentRoute from '../route.js';

/** AirLib managed */
const route = parentRoute.route('/demos', { sitemap: false });
/** AirLib managed */

export const demosRoute = route;

export default demosRoute;
